# Firebase Configuration (single project, multiple auth instances)
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
VITE_FIREBASE_MEASUREMENT_ID=

# Firebase Cloud Messaging (FCM) - untuk Web Push Notifications
# Generate VAPID key di Firebase Console > Project Settings > Cloud Messaging > Web Push certificates
VITE_FIREBASE_VAPID_KEY=

# ========================================
# VERCEL ENVIRONMENT VARIABLES (Server-side)
# ========================================
# Set di Vercel Dashboard > Project Settings > Environment Variables

# Firebase Service Account JSON (untuk Vercel API routes)
# 1. Buka Firebase Console > Project Settings > Service Accounts
# 2. Klik "Generate new private key"
# 3. Copy SELURUH isi JSON file ke variable ini (satu baris, tanpa enter)
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"...",...}

# Firebase Project ID (opsional jika sudah ada di service account)
# FIREBASE_PROJECT_ID=your-project-id

VITE_CLOUDINARY_CLOUD_NAME=
VITE_CLOUDINARY_UPLOAD_PRESET=

# ⚠️ JANGAN tambahkan VITE_CLOUDINARY_API_KEY atau API_SECRET di sini!
# Cloudinary unsigned upload hanya butuh cloud name + upload preset
# API Key/Secret hanya untuk server-side operations (Vercel functions)

# ========================================
# BigModel AI Configuration
# ========================================

# LOCAL DEV MODE (⚠️ ONLY for local testing)
# Set to 'true' untuk pakai direct API call (API key exposed!)
# Set to 'false' atau hapus untuk production mode (Vercel API routes)
VITE_USE_LOCAL_API=false

# API key untuk LOCAL DEV ONLY (⚠️ EXPOSED - hanya untuk testing!)
# JANGAN enable di production!
# VITE_BIGMODEL_API_KEY=your-api-key-here

# ========================================
# PRODUCTION DEPLOYMENT (Vercel)
# ========================================
# Set di Vercel Dashboard → Environment Variables:
# Name: BIGMODEL_API_KEY (tanpa VITE_ prefix!)
# Value: your-api-key-here
# Environment: Production, Preview, Development

# ========================================
# LOCAL DEVELOPMENT ONLY
# ========================================
# For local development, create .env.local file and add:
# BIGMODEL_API_KEY=sk-your-api-key-here
