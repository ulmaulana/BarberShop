# Firebase Admin SDK (auto-initialized in emulator)
# No manual config needed for development

# Production: Set service account key path
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/serviceAccountKey.json

# Firestore region (default: auto-detect)
# FIRESTORE_EMULATOR_HOST=localhost:8080

# Email service (optional)
# SENDGRID_API_KEY=
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASS=

# Payment gateway (optional)
# MIDTRANS_SERVER_KEY=
# MIDTRANS_CLIENT_KEY=
# MIDTRANS_IS_PRODUCTION=false
